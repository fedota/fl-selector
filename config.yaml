SELECTOR_ID: "1" # container name / localhost
SELECTOR_SERVICE: # not set when running locally
PORT: "50051" # use different port when running locally
COORDINATOR_ADDRESS: "localhost:50050"
FL_ROOT_PATH: "../data"
INIT_FILES_PATH: "/initFiles/"
MODEL_FILE: "model.h5"
AGG_CHECKPOINT_FILE_PATH: "/fl_agg_checkpoint"
AGG_CHECKPOINT_WEIGHT_PATH: "/fl_agg_checkpoint_weight"
ROUND_CHECKPOINT_UPDATES_DIR: "/roundFiles/checkpoint_"
ROUND_CHECKPOINT_WEIGHT_DIR: "/roundFiles/checkpoint_weight_"
CHUNK_SIZE: 65536 #64 * 1024
POST_CHECKIN_RECONNECTION_TIME: 8000
POST_UPDATE_RECONNECTION_TIME: 8000
ESTIMATED_WAITING_TIME: 20000